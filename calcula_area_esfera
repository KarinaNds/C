#include <stdio.h>

int main() {
    const double PI = 3.141592; 
    double raio, area;
    
    printf("Digite o valor do raio da esfera: ");
    scanf("%lf", &raio);
 
    area = 4 * PI * raio * raio;

    printf("A área da superfície da esfera é: %.3lf\n", area);

    return 0;
}
